# 智能研究助手Agent：高频追问题库 V2

## 1. 这份题库怎么用

这份题库不是为了死记硬背，而是为了把项目追问压缩成少数几个“母题”来准备。

V2 题库的重点，是让你的回答同时满足三件事：

- 有亮点
- 有边界
- 能落回实现

## 2. 母题一：这个项目到底解决了什么问题

### 面试官在考什么

- 你是不是只做了一个 AI Demo
- 你能不能说清真实需求
- 你有没有把项目做成系统而不是功能堆砌

### 常见变种

- 你这个项目和普通聊天机器人有什么区别
- 为什么科研场景需要单独做一个系统
- 为什么不用直接用 ChatGPT

### 参考回答

`这个项目解决的不是单轮问答，而是科研任务中的长期上下文连续性、证据可追溯、结构化写作、研究路线辅助和结果复用问题。普通聊天系统更适合即时问答，但在科研场景里，用户会持续上传论文、围绕同一研究方向多轮追问、生成周报或阅读笔记，并希望系统记住研究进度和偏好。`

`所以我把它做成了一个面向科研流程的智能研究助手系统，而不是单纯的聊天入口。`

## 3. 母题二：为什么采用 `Supervisor + 多Agent`

### 面试官在考什么

- 你是不是为了追热点才上多Agent
- 你是否理解多Agent的收益和成本
- 你的架构边界是否清楚

### 常见变种

- 为什么要 Supervisor
- 为什么不是单Agent
- 多Agent 的必要性是什么

### 参考回答

`我保留多Agent不是为了堆概念，而是因为复杂科研任务天然需要分工协作。系统由 Supervisor Agent 持有全局上下文，并负责任务复杂度判断、路径选择、上下文裁剪和结果汇总。对于简单任务，Supervisor 直接处理；对于复杂任务，再由多个专用子Agent协同执行。`

`这里的多Agent不是完全去中心化自由协商网络，而是 Supervisor 主导下的自治协作。自治主要体现在子Agent在职责域内能够自主完成推理、工具调用、观察和局部修正。`

### 一句压缩版

`多Agent 是复杂任务下的核心能力，自治发生在职责域内，收敛由 Supervisor 负责。`

## 4. 母题三：`plan-execute` 和 `ReAct` 为什么同时存在

### 面试官在考什么

- 你是否分清了规划层和执行层
- 你是否理解不同 Agent 范式的边界

### 常见变种

- 为什么不全都用 ReAct
- `plan-execute` 和 `ReAct` 的区别是什么
- 它们会不会冲突

### 参考回答

`两者不冲突，因为它们解决的是不同层次的问题。plan-execute 放在 Supervisor 层，负责复杂任务的拆解、排序、调度和收敛；ReAct 放在子Agent 执行层，负责单个子任务里的推理、工具调用和观察迭代。`

`简单理解就是，plan-execute 决定“怎么拆、怎么排”，ReAct 决定“拿到子任务后怎么做”。`

`这里我没有再单独拆一个 Planning Agent，因为任务规划本身就是 Supervisor 的职责。否则会出现“Supervisor 负责编排，Planning Agent 也负责规划”的角色重叠，边界反而会变糊。`

## 5. 母题四：为什么简单任务不直接也走多Agent

### 面试官在考什么

- 你有没有成本意识
- 你有没有复杂度治理意识

### 常见变种

- 全部多Agent是不是更强
- 为什么还保留简单任务直解
- 多Agent 会不会增加延迟

### 参考回答

`不是所有任务都值得进入多Agent。简单任务如果强行拆分，会带来额外的上下文切分、通信成本、调度延迟和失败点，但不会带来明显收益。尤其是论文问答、短链路解释和基于当前上下文的追问，Supervisor 已经可以直接完成。`

`所以系统采用短链路优先、复杂任务再进入 plan-execute 的策略。`

## 6. 母题五：`L1/L2/L3` 分层记忆为什么这样设计

### 面试官在考什么

- 你是否理解不同时间尺度的信息治理
- 你有没有认真做上下文工程

### 常见变种

- L1/L2/L3 分别存什么
- 为什么不把所有内容都塞进一个记忆库
- USER、SOUL、Research_state 和每日记忆的边界是什么
- 长期记忆是什么时候写入的

### 参考回答

`L1 负责短期会话连续性，维护当前任务状态、近期事实和结构化 working memory；L2 是全局认知层，由 USER.md、SOUL.md 和 Research_state.md 构成，用来保存用户偏好、系统行为约束和研究阶段状态；L3 是长期记忆，主要通过每日记忆.md 沉淀研究过程、阶段结论和可复用经验。`

`分层的核心价值，是把不同时间尺度的信息分开治理，避免上下文污染，也避免长期信息失效。这里 L3 不是聊天备份层，而是通过事件触发式抽取沉淀形成的长期研究资产。`

## 7. 母题六：为什么 `RAG` 不等于记忆系统

### 面试官在考什么

- 你是否真的理解 memory 和 retrieval 的边界
- 你的项目有没有超出“知识库问答”

### 常见变种

- RAG 和长期记忆是什么关系
- 为什么 RAG 要单独成层
- 当前回答时哪些内容来自 memory，哪些来自 retrieval

### 参考回答

`Memory 回答的是“系统应该长期保留什么”，RAG 回答的是“当前任务需要召回什么证据”。所以 L1/L2/L3 是长期上下文体系的一部分，而 RAG 是一个按需触发的检索补全层，用来从论文和长期资料中召回与当前任务最相关的证据片段。`

`它们会协同工作，但职责完全不同。`

## 8. 母题七：你这个 RAG 的关键设计点是什么

### 面试官在考什么

- 你是否真的做过 RAG
- 你能否说明质量优化逻辑

### 常见变种

- 你的检索链路是什么
- 为什么做混合检索
- chunk 怎么切
- rerank 为什么需要
- 你怎么证明优化有效

### 参考回答

`我关注的不是只把向量库接起来，而是让 Paper RAG 真正服务研究任务。当前最关键的设计点包括：overlap-aware chunking、intent-aware query planning、hybrid retrieval、explainable lightweight rerank、retrieval trace 和一个固定样本的小型 offline benchmark snapshot。`

`它的目标不是找相似文本，而是为当前任务提供可追溯、可引用、可判断充分性的证据。`

### 带数字的回答

`当前 snapshot 的样本数是 10，Top1 hit rate 从 0.70 提升到 0.90，多语言命中从 0.00 提升到 1.00。它主要用于验证优化方向有效，而不是充当完整泛化评测。`

### 延伸追问

- 为什么不把长期记忆直接并入论文 `RAG`
- 历史研究记忆和论文证据怎么协同

### 延伸回答

`我不会把 L3 直接并入论文 RAG，因为长期记忆和论文原文不是同一类对象。L3 提供的是历史研究上下文，论文 RAG 提供的是当前回答证据。如果无差别混排，系统自己过去写下来的总结可能压过论文原文，进而破坏 Evidence Boundary。更合理的设计是给 L3 建一条独立的 semantic/hybrid memory recall 链路，再由 Supervisor 决定是走 memory recall、paper RAG 还是双检索。`

## 9. 母题八：你怎么控制幻觉、拒答和降级

### 面试官在考什么

- 你是否有可靠性意识
- 你是不是只会做“能跑起来”的 Demo

### 常见变种

- 怎么降低幻觉
- 证据不足怎么办
- 为什么有时要拒答
- 你如何平衡效果和可信度

### 参考回答

`我主要从证据边界、路径约束和失败处理三层控制幻觉。首先系统会先构造证据，再判断证据充分性，而不是直接生成。其次系统会显式区分 SUFFICIENT、WEAK 和 NONE，再决定走 grounded answer、保守回答、web supplement 还是 refusal。最后再配合降级和状态机，把失败点暴露出来，避免系统在证据不足时硬编。`

## 10. 母题九：长期记忆怎么沉淀，又怎么避免重复和污染

### 面试官在考什么

- 你是否真正思考过长期记忆治理
- 你有没有把所有聊天都直接写进去

### 常见变种

- 什么内容会进入长期记忆
- 长期记忆什么时候写入
- 会话压缩时怎么沉淀
- 为什么不会重复写入同一批内容

### 参考回答

`长期记忆最大的难点不是能不能写进去，而是怎么把研究过程稳定沉淀下来，同时避免重复和失控。我没有采用“每轮回答后直接把内容落盘”的方式，而是采用事件触发式长期沉淀：用户显式要求记住时，Supervisor 按内容类型写入 L2 或 L3；会话临近 compaction 时，Supervisor 只对 turnId 大于 lastDepositedTurnId 的新增对话区间做一次增量抽取，并把抽取后的高价值研究条目写入每日记忆；会话关闭时只做补偿式 flush。`

`为了避免重复沉淀，我会记录 lastDepositedTurnId，保证下一次 flush 只处理新增 turn；写入每日记忆前再做一次轻量 merge，如果和最近一条条目高度相近就合并，不再 append。这样 L3 存的不是完整对话，而是抽取后的长期研究沉淀。`

## 11. 母题十：点赞点踩为什么不是附属功能

### 面试官在考什么

- 你是否从长期系统角度思考问题
- 你有没有让系统越用越有价值的意识

### 常见变种

- 点赞点踩为什么值得做
- feedback 怎么参与后续检索
- 用户反馈如何影响后续召回排序

### 参考回答

`科研任务是长期连续发生的，如果系统只解决单次问答，而不会根据用户对证据质量的反馈去调整后续检索行为，它的长期价值会很弱。`

`所以我把点赞点踩做成检索闭环的一部分。当前回答会保留本轮用到的关键论文证据 chunk，用户点赞或点踩后，系统会把反馈映射到这些 chunk 的 feedbackScore 上；后续相似任务再次检索时，这个分数会作为额外排序信号参与召回与重排。这样系统不是抽象地“学用户偏好”，而是直接优化论文证据层质量。`

## 12. 母题十一：L3 语义记忆检索什么时候触发

### 面试官在考什么

- 你有没有认真做检索路由设计
- 你是否理解历史上下文和外部证据的边界

### 常见变种

- 为什么不默认同时查两个库
- 什么时候只查长期记忆
- 什么时候同时查记忆和论文

### 参考回答

`我不建议默认同时查两个库，而是让 Supervisor 先做检索路由。最自然的四种模式是 No Retrieval、Memory Recall Only、Paper RAG Only 和 Memory + Paper。`

`其中 Memory Recall Only 适合“之前讨论过什么”“最近进展到哪了”这类历史连续性问题；Paper RAG Only 适合需要 grounded evidence 的论文问答；只有在“基于历史结论继续结合外部证据分析”这类复杂任务里，才进入 Memory + Paper。`

`双检索默认也不建议并行，而是 Memory First -> Paper Second。先查 L3 拿到历史研究上下文，再据此改写 query 或缩小论文检索范围，这样 precision 更高，也更符合系统的证据边界设计。`

## 13. 母题十二：上传状态机为什么重要

### 面试官在考什么

- 你是否有后端工程化思维
- 你能否把 AI 功能做成可治理的系统

### 常见变种

- 为什么上传还要状态机
- 解析失败和索引失败怎么区分
- 错误怎么定位

### 参考回答

`因为论文接入不是简单上传文件，而是一个包含存储、解析、切块、索引的多阶段链路。当前系统把它建模成显式状态机：UPLOADED -> PARSING -> INDEXING -> INDEXED / FAILED，并保留 failureStage 和 parseError 来定位失败阶段。`

`这样它才像一个可调试、可治理的后端系统。`

## 14. 母题十三：这个项目最难的点是什么

### 面试官在考什么

- 你是否真的做过这个项目
- 你能否抽象出真正难的问题

### 参考回答

`我觉得最难的点不是某一个模块，而是如何把编排、记忆、检索、证据边界、结构化写作和反馈闭环真正串成一个自洽系统。单独做一个 RAG Demo 或 Agent Demo 不难，但要让系统在科研任务里长期稳定工作，真正难的是上下文治理、证据边界控制和复杂任务收敛。`

## 15. 最后压缩成 8 个必须练熟的母题

1. 这个项目解决了什么真实问题
2. 为什么是 `Supervisor + 多Agent`
3. `plan-execute` 和 `ReAct` 的边界
4. 为什么简单任务不默认多Agent
5. `L1/L2/L3` 分别做什么
6. 为什么 `RAG` 不是记忆系统
7. L3 语义记忆检索什么时候触发，为什么不直接并库
8. 这个项目最大的技术价值是什么

把这 8 个问题练熟，大部分项目追问都能沿着这条主线接住。

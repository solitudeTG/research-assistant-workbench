# F002 到 F003 开发交接

## 当前状态

- Harness 基线已建立，Markdown 文档是事实源。
- F002 已定位为 Epic / 总规格 / 总路线图。
- 开发前 Vision Gate 结论为 `needs follow-up`：进入代码前必须拆 F003+ 子 Feature，并先提交 Harness 基线。
- 子 Feature F003-F011 已创建，F003 是首个开发切片。

## 新会话启动顺序

1. 读取 `docs/BACKLOG.md`。
2. 读取 `docs/features/F002-next-generation-research-workbench.md`。
3. 读取 `docs/reviews/VG-001-f002-before-development.md`。
4. 读取 `docs/features/F003-project-workbench-model-api.md`。
5. 读取 `docs/plans/F002-next-generation-research-workbench-plan.md` 的 Task 1。
6. 从 F003 开始 TDD 实现。

## 首个开发目标

F003 项目级数据模型与基础 API：

- 创建项目级 schema。
- 实现 `ProjectRepository` 和 `ProjectController`。
- 通过 `ProjectRepositoryTest` 与 `ProjectControllerTest`。

## 注意事项

- 不要继续以旧 `sessionKey + documentId` 作为架构中心。
- 不要在 F003 中实现 Redis Stream、Agent 编排、资料状态机或 UI。
- 每个子 Feature 完成后必须留下验证证据，并更新对应 Feature。
- 如果决定不实现 Redis Stream，必须先新增 ADR。
